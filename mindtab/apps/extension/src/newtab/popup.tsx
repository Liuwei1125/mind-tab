import React from 'react';
import ReactDOM from 'react-dom/client';
import { PopupPage } from './pages/PopupPage';
import '@mindtab/ui/styles/globals.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PopupPage />
  </React.StrictMode>
);
